# Coding guidelines
- Follow clean code principles: small functions, descriptive names, no premature abstraction
- Write a test for every new function or component before considering it done
- Use TypeScript strict mode
- Prefer composition over inheritance
- This is a prototype project, but if we manage to create something good, it will be the basis for the real app
- I (the developer) am new to app development and React.